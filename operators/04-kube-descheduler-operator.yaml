# Cluster Kube Descheduler Operator
# InstallPlan approval order: 4 of 4
# KubeDescheduler CR is in ../instances/ — sync after descheduler operator CSV is Succeeded.
---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: cluster-kube-descheduler-operator
  namespace: openshift-kube-descheduler-operator
  labels:
    app.kubernetes.io/name: cluster-kube-descheduler-operator
    app.kubernetes.io/component: operator
  annotations:
    argocd.argoproj.io/sync-wave: "40"
    ocpvirt-workloads-ha/install-plan-order: "4"
spec:
  channel: stable
  installPlanApproval: Manual
  name: cluster-kube-descheduler-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  config:
    nodeSelector:
      node-role.kubernetes.io/worker: ""
    tolerations: []
